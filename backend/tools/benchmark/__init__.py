"""PERF-001 — Automated LLM benchmark suite for SQL-generation model comparison.

Engineering tool only: reuses the production pipeline read-only (dependency
injection of a per-model provider) and never modifies runtime behavior.
"""
